%YAML 1.1
---
$schema: asdf://stsci.edu/datamodels/roman/schemas/rad_schema-1.0.0
id: asdf://stsci.edu/datamodels/roman/schemas/reference_files/integralinverselinearity-1.0.0

title: Integral Inverse Linearity Correction Reference Schema

datamodel_name: IntegralinverselinearityRefModel

type: object
properties:
  meta:
    allOf:
      - $ref: asdf://stsci.edu/datamodels/roman/schemas/reference_files/ref_common-1.2.0
      - type: object
        properties:
          reftype:
            type: string
            enum: [INTEGRALNONLINEARITY]
          n_channels:
            title: Number of channels
            description: |
              The number of channels.
            type: integer
          n_pixels_per_channel:
            title: Number of Pixels per Channel
            description: |
              The number of the pixels per channel.
            type: integer
        required: [n_channels, n_pixels_per_channel]
  inl_table:
    type: object
    patternProperties:
      "^[0-9]{,2}$":
        type: object
        properties:
          channel:
            title: Channel Number
            description: |
              Channel Number
            type: integer
          correction:
            title: Channel Correction
            description: |
              Channel Correction
            anyOf:
            - type: "null"
            - tag: tag:stsci.edu:asdf/core/ndarray-1.*
              datatype: float64
              exact_datatype: true
              ndim: 1
        required: [channel, correction]
  value:
    title: Integral Inverse Linearity
    description: |
      Integral Inverse Linearity
    anyOf:
      - type: "null"
      - tag: tag:stsci.edu:asdf/core/ndarray-1.*
        datatype: uint16
        exact_datatype: true
        ndim: 1
required: [meta, inl_table, value]
flowStyle: block
propertyOrder: [meta, inl_table, value]
